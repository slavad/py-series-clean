from spec.spec_helper import *
import clean.iterator as itr

with description(itr.Iterator) as self:
    with description('__init__'):
        #TODO: test helper methods used here
        with it('sets all values'):
            pass